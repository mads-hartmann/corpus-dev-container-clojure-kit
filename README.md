# Clojure Kit Corpus App

Minimal Kit web server for the devcontainer setup evaluation corpus.

## Run

```bash
clojure -M:run
```

The server listens on `0.0.0.0:${PORT:-3000}`.

## Endpoints

- `GET /`
- `GET /healthz`
